rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Permitir lectura y escritura en TODO mientras estamos en desarrollo
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
